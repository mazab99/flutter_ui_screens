import 'package:flutter/material.dart';

import '../widgets/headers.dart';

class HeadersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    //return const Scaffold(body: Header10());
    //return const Scaffold(body: Header9());
    //return const Scaffold(body: Header8());
    //return const Scaffold(body: Header7());
    //return const Scaffold(body: Header6());
    //return const Scaffold(body: Header5());
    //return const Scaffold(body: Header4());
    //return const Scaffold(body: Header3());
    //return const Scaffold(body: Header2());
    //return const Scaffold(body: Header10());
    return const Scaffold(body: Header1());

  }
}
